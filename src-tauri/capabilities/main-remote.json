{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-remote",
  "description": "Grant WhatsApp Web only the bridge commands it needs plus opening trusted local settings. core:default and plugin APIs are intentionally not granted.",
  "windows": ["wa-*"],
  "remote": {
    "urls": ["https://web.whatsapp.com/*"]
  },
  "permissions": [
    "allow-notify",
    "allow-set-unread",
    "allow-dlog",
    "allow-open-settings"
  ]
}
